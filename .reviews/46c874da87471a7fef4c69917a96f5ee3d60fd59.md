# Retrospective adversarial review — linear transport parsing

Reviewed 2026-09-05 after the deployment gate exposed missing July review evidence. Claude `/review` was unavailable because the host OAuth session had expired.

- **Dangerous assumption:** the bounded scan recognizes enough YAML shapes without becoming a YAML parser.
- **Missing failure mode:** unusual quoting or multiline YAML may remain unrecognized and produce an absent transport hint.
- **Boundary pressure:** a conservative hint extractor must not be described as full Smithery YAML validation.

Disposition: accepted. The parser fails conservatively, input size is bounded, and regression tests cover supported hints, unrelated keys, and a repeated-key adversarial input.
